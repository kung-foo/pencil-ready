// Decimal × single-digit-whole multiplication. Vertical stack with the
// decimal-only top operand, the whole-number multiplier below, and a
// product line that re-introduces the decimal point. Single answer-row —
// no partial products (multiplier is always 1 digit).
#import "/lib/problems/_layouts/decimal-vertical-stack.typ": decimal-vertical-stack-problem as decimal-multiply-problem
